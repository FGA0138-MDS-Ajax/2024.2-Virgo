import React, { StyleSheet, TouchableOpacity } from "react";
import { Text, View } from "react-native";

export default function instructions() {
  return (
    <View>
      <Text>instructions</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
